<UserControl x:Class="SolusManifestApp.Views.GBEDenuvoControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Border Style="{StaticResource ModernCardStyle}" Padding="30" HorizontalAlignment="Left" MaxWidth="800">
            <StackPanel>
            <TextBlock Text="GBE Token Generator"
                       FontSize="24"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimaryBrush}"
                       Margin="0,0,0,20"/>

            <TextBlock Text="Generate Goldberg Emulator tokens for games. This requires Steam to be running and the game to be owned by the logged-in account."
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       FontSize="13"
                       TextWrapping="Wrap"
                       Margin="0,0,0,20"/>

            <!-- App ID Input -->
            <TextBlock Text="Steam App ID"
                       Foreground="{DynamicResource TextPrimaryBrush}"
                       FontSize="14"
                       Margin="0,0,0,5"/>
            <TextBox Text="{Binding AppId, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                     FontSize="14"
                     Padding="10"
                     Margin="0,0,0,15"
                     Height="35"
                     Background="{DynamicResource CardBackgroundBrush}"
                     Foreground="{DynamicResource TextPrimaryBrush}"/>

            <!-- Output Path -->
            <TextBlock Text="Output Directory"
                       Foreground="{DynamicResource TextPrimaryBrush}"
                       FontSize="14"
                       Margin="0,0,0,5"/>
            <Grid Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Text="{Binding OutputPath, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                         FontSize="14"
                         Padding="10"
                         Height="35"
                         IsReadOnly="True"
                         Margin="0,0,10,0"
                         Background="{DynamicResource CardBackgroundBrush}"
                         Foreground="{DynamicResource TextPrimaryBrush}"/>

                <Button Grid.Column="1"
                        Content="📁 Browse"
                        Command="{Binding BrowseOutputPathCommand}"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Padding="20,10"
                        Height="35"/>
            </Grid>

            <!-- Generate Button -->
            <Button Content="🔑 GENERATE TOKEN"
                    Command="{Binding GenerateTokenCommand}"
                    Style="{StaticResource ModernButtonStyle}"
                    FontWeight="Bold"
                    FontSize="14"
                    Padding="20,12"
                    Margin="0,5,0,15"
                    IsEnabled="{Binding IsNotGenerating}"/>

            <!-- Progress Indicator -->
            <ProgressBar IsIndeterminate="True"
                        Height="4"
                        Margin="0,0,0,15"
                        Visibility="{Binding IsGenerating, Converter={StaticResource BooleanToVisibilityConverter}}"/>

            <!-- Log Output -->
            <TextBlock Text="Log Output"
                       Foreground="{DynamicResource TextPrimaryBrush}"
                       FontSize="14"
                       Margin="0,0,0,5"/>
            <Border Background="{DynamicResource CardBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="10"
                    Height="250">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Disabled">
                    <TextBlock Text="{Binding LogOutput}"
                              FontFamily="Consolas"
                              FontSize="11"
                              Foreground="{DynamicResource TextSecondaryBrush}"
                              TextWrapping="Wrap"/>
                </ScrollViewer>
            </Border>

            <!-- Credits section -->
            <Border Background="{DynamicResource CardBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="15"
                    Margin="0,15,0,0">
                <StackPanel>
                    <TextBlock Text="Credits"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               Margin="0,0,0,8"/>
                    <TextBlock Foreground="{DynamicResource TextSecondaryBrush}"
                               FontSize="12"
                               TextWrapping="Wrap">
                        <Run Text="GBE Token Generator developed by:"/>
                        <LineBreak/>
                        <Run Text="• Detanup01" FontWeight="Medium"/>
                        <LineBreak/>
                        <Run Text="• NotAndreh" FontWeight="Medium"/>
                        <LineBreak/>
                        <Run Text="• Oureverday" FontWeight="Medium"/>
                    </TextBlock>
                </StackPanel>
            </Border>
        </StackPanel>
        </Border>
    </ScrollViewer>
</UserControl>
